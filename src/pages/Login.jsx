import React from "react";
import FormTemplate from "../components/FormTemplate";

const Login = (props) => {
    
    return (
        <React.Fragment>
            <FormTemplate type={false} />
        </React.Fragment>
    );
};

export default Login;
